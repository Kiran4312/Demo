pipeline {
    agent any
    stages {
        stage('stage 1') {
            steps {
            sh 'env | sort'
          }
        }
    }
    post {
        always {
            sh 'shell.sh'
        }
        failure {
            script {
                sh 'echo "This job failed"'
            }
        }
    }
}
