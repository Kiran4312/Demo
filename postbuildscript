pipeline {
    agent any
    stages {
        stage('stage 1') {
            steps {
            sh 'env | sort'
          }
        }
    }
    post {
        success {
            sh 'shell.sh'
        }
        failure {
            script {
                sh '''
                |echo "This job failed"
                |echo "And I am not sure why"
                '''
            }
        }
    }
}
